{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "placement",
  "description": "A boogu image-edit prompt that places the extracted product into a themed scene. references[0] is the product (from the matte). Consumed by comfy.boogu.",
  "type": "object",
  "required": ["imagePrompt", "references"],
  "properties": {
    "imagePrompt": {
      "type": "string",
      "minLength": 30,
      "description": "The scene + placement edit prompt. The product is image1 (the first reference)."
    },
    "references": {
      "type": "array",
      "minItems": 1,
      "maxItems": 4,
      "items": {
        "type": "object",
        "required": ["id", "type"],
        "properties": {
          "id": { "type": "string" },
          "type": { "type": "string" }
        }
      }
    }
  }
}
